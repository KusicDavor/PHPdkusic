{{ form_start(form) }}
<div class="form-group">
	{{ form_widget(form) }}
</div>
<button style="margin" type="submit" class="btn btn-primary">Save</button>
<a href="{{ path('app_user_index') }}" class="btn" style="font-size: 13px">Back to List</a>
{{ include('user/_delete_form.html.twig') }}
{{ form_end(form) }}
