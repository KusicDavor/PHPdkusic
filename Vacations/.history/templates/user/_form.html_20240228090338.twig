{{ form_start(form) }}
<div class="form-group">
	{{ form_widget(form) }}
</div>
<button type="submit" class="btn btn-primary">Save</button>
<a href="{{ path('app_user_index') }}" class="btn" style="text-align: center; padding: 5px 8px; font-size: 15px">Back to List</a>
{{ include('user/_delete_form.html.twig') }}
{{ form_end(form) }}
