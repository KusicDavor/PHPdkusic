{% extends 'user/base.html.twig' %}

{% block title %}User
{% endblock %}

{% block body %}
	<h1>User</h1>

	<table class="table">
		<tbody>
			<tr>
				<th style="width:50px">Email</th>

				<td>{{ user.email }}</td>
			</tr>
			<tr>
				<th>Role</th>
				<td>{{ user.roles ? user.role.roleName }}</td>
			</tr>
		</tbody>
	</table>

	<a href=" {{ path('app_user_index') }} ">back to list</th>
</tr></tbody></a><a href="{{ path('app_user_edit', {'id': user.id}) }}">edit</a>{{ include('user/_delete_form.html.twig') }}{% endblock %}
