{{ form_start(form) }}
<div class="form-group">
	{{ form_widget(form) }}
</div>
<button type="submit" class="btn btn-primary">Save</button>
<a href="{{ path('app_user_index') }}" class="btn" style="text-align: center; padding: 7px 10px; font-size: 15px">Back to List</a>


{{ form_end(form) }}
