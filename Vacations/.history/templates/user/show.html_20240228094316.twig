{% extends 'user/base.html.twig' %}

{% block title %}User
{% endblock %}

{% block body %}
<h1>User</h1>

<table class="table">
	<tbody>
		<tr>
			<th class="attributes">Email</th>

			<td>{{ user.email }}</td>
		</tr>
		<tr>
			<th>Role</th>
			<td>{{ user.roles ? user.role.roleName }}</td>
		</tr>
	</tbody>
</table>

<a style="margin-top: 10px; font-size: 13px" class="btn btn-primary" href=" {{ path('app_user_index') }} ">Back to list</th></tr></tbody></a><a style="font-size: 13px" class="btn btn-primary" href="{{ path('app_user_edit', {'id': user.id}) }}">Edit</a><a {{ include('user/_delete_form.html.twig') }} </a> {% endblock %}
