{{ form_start(form) }}
<div style="margin-left: 10px">
	{{ form_widget(form) }}
</div>
<button class="btn">{{ button_label|default('Send request') }}</button>
<a style="margin-top: 10px; margin-left: 10px; font-size: 12px" class="btn btn-primary" href=" {{ path('app_vacation_request_index') }} ">Back to list</a>
{{ form_end(form) }}
