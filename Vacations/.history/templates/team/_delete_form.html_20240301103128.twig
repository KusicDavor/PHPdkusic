<form method="post" action="{{ path('app_team_delete', {'id': form->getData()}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ team.id) }}">
	<button class="btn" style="background-color: red; font-size: 14px">Delete</button>
</form>
